/** @type {import('next').NextConfig} */
const nextConfig = {
    output: "export",

    basePath: "/aditya17varma.github.io",

    images: {
        unoptimized: true,
    },
};

module.exports = nextConfig
